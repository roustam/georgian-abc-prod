// header comp

import {Fragment} from "react";

const Header = ({ headerTitle }) => {

    return (
        <Fragment>
            <h2>{ headerTitle }</h2>
        </Fragment>
    )
}

export default Header